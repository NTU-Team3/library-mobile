import React from "react";
import VerticalList from "./VerticalList";

const PoliticalBook = ({ data }) => {
  return <VerticalList title="Psychology" data={data} />;
};

export default PoliticalBook;
